import React, {Fragment} from 'react';

export const ReactComponentAsNpmModule = () => (
  <Fragment>
    <h2>This is the component that you just installed.</h2>
    <p>The src folder is the one that you should update with your actual component.</p>
    <small>(You can probably delete this one now).</small>
  </Fragment>
);
